// src/modules/notifications/index.ts
// Minimal notifications schema — used by auth module

export { notifications } from './schema';
export type { Notification, NotificationInsert } from './schema';
